# Base URL for the backend API (must be HTTPS in production)
# Example: https://tourcheetah-api.up.railway.app
VITE_API_URL=

# Demo mode for static hosting (GitHub Pages). If true, Login/Register will not call the API.
VITE_DISABLE_AUTH=false
